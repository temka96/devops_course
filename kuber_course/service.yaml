---
apiVersion: v1
kind: Service
metadata:
  name: my-app-svc
  namespace: application
spec:
  selector:
    app: my-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
  # type: LoadBalancer
      
      
